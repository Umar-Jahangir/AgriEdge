# Pest models (local artifacts)

IP102-based pest detection/classification weights will be stored here after Phase 2 training.

- Large files (`*.pt`, `*.onnx`, `runs/`) are gitignored.
- Track evaluation reports under `docs/` when available.
- Do not train until disease Phase 1 is complete on the GPU laptop workflow.
