# Nitrogen models (local artifacts)

Maize nitrogen-deficiency classifier weights will be stored here after the dataset is acquired.

Expected dataset path (local only, not in git): `data/raw/maize_nitrogen/`

Large model files are gitignored.
