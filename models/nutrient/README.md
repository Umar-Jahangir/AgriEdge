# Nutrient models (local artifacts)

Maize nutrient-deficiency classifier weights will be stored here after the dataset is fully acquired and prepared.

Expected dataset path (local only, not in git): `data/raw/maize_nutrient/`

Large model files are gitignored.
